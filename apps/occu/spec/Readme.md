# Occu: specification

This folder contains a spec for calculating taxes using FIFO.

The spec is written in python. The TS code originates from the spec and has minor modifications that reflect the specific use cases in `occu`.
